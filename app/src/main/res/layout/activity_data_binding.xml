<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="net.hogelab.android.androidui.databinding.viewmodel.DataBindingRootViewModel" />

        <variable
            name="viewModel"
            type="DataBindingRootViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/content_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_margin="8dp"
            android:textAlignment="center"
            android:text="@{viewModel.contentTitle}"/>

        <TextView
            android:id="@+id/content_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/content_title"
            android:layout_margin="8dp"
            android:textAlignment="center"
            android:text="@{viewModel.contentSubtitle}"/>

        <FrameLayout
            android:id="@+id/data_binding_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/content_subtitle"
            tools:context=".diagnostics/DataBindingActivity"
            tools:ignore="MergeRootFrame" />

    </RelativeLayout>

</layout>